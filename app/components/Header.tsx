import React from 'react';
import { Box, AppBar, Toolbar, Button } from '@mui/material';
const Header = () => {
    return (
        <Box>
            <AppBar>
                <Toolbar>

                </Toolbar>
            </AppBar>
        </Box>
    );
};

export default Header;